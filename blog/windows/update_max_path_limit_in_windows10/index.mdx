---
title: "修改windows10最大系统路径限制"
date: "2022-11-25"
tags: ["windows"]
---

## 这篇文章讲什么？

如何在 Windows 10 中更改默认的 256 个字符路径长度限制(MAX_PATH)。

## 系统环境及要求

Windows 10 (1607)及更高版本。

## 有什么用？

默认情况下，Windows 使用 256 个字符的路径长度限制(MAX_PATH):命名文件、路径和名称空间。在程序中访问系统中的文件，如果路径过长会导致读取访问失败。

## 正文

在 Windows 10 中启用长路径行为：

1. 转到 Windows“开始”,然后键入`REGEDIT`。
2. 选择“注册表编辑器”。
3. 在“注册表编辑器”中，导航到以下位置：`HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\FileSystem`。
4. 选择名为 LongPathsEnabled 的条目。

- 注意：如果注册表项不存在，则还可通过执行以下操作添加该条目：
  1. 选择 FileSystem 文件夹后，在注册表项所在的“名称”列的空白处单击鼠标右键。
  2. 选择新建。
  3. 选择 DWORD （32 位）值。
  4. 在新添加的关键点上单击鼠标右键，然后选择“重命名”。
  5. 将键命名为 LongPathsEnabled。
  6. 按 Enter。

第一次调用受影响的 Win32 文件或目录功能后，注册表项的值将被系统（按进程）缓存。在进程的生命周期内不会重新加载注册表项。为了使系统上的所有应用都识别密钥的值，可能需要重新启动计算机，因为某些进程可能已在设置密钥之前开始。
